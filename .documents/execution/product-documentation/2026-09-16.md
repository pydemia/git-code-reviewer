# 제품 문서와 화면 안내 보강 — 2026-09-16

GCR와 CD의 README, 설치·설정·연결·사용 예시, 아키텍처, 기능명세와 화면 설명을 현재 구현에 맞췄다. 기존 기능의 안내 작업이며 모델·인증·데이터·사용자 설정·runner는 변경하지 않았다.

## 문서와 화면

- GCR: [문서 안내](../../../docs/README.md), [설치·연결](../../../docs/product/getting-started.md), [아키텍처](../../../docs/product/architecture.md), [기능 목록](../../../docs/product/features.md). 앱의 `/getting-started`, `/architecture`, `/features`, `/guide`에서 읽을 수 있다. 프로필 연결과 리뷰 이력·지침 입력 화면에도 작업 순서를 넣었다.
- CD: `vscode-extension/docs/installation.md`, `how-to.md`, `architecture.md`, `features.md`를 README에서 연결했다. 연결 상태, 이력 조회, Summary의 Evidence에 원문·지침·모델 판단을 구분하는 설명을 넣었다.
- 설치 예시는 Compose/소스/Helm과 VSIX를 구분한다. reader key·GitHub PAT·모델 credential, web TLS·DB TLS, 수동 리뷰·자동 service·legacy hook도 구분한다.
- 고정 Astra/xhigh, API provider의 수동 리뷰 미지원, 모든 P3의 commit 차단이라는 오래된 설명을 수정했다. 모델 추론이 반드시 기기 안에서 실행된다는 표현도 피했다.

## 검증

[기계 판독 근거](2026-09-16-evidence.json)에 결과와 소스 SHA를 기록했다.

- GCR web build와 변경 TypeScript ESLint 통과. 기존 보고서·등급 회귀 7건과 클라이언트 credential Chrome 회귀 5건 통과.
- Chrome에서 실제 React/Markdown 페이지 7종을 1360px·390px로 표시했다. API 응답은 fixture이다. 14개 화면에서 JavaScript 오류와 페이지 가로 넘침이 없었고 목차의 모든 anchor가 존재했다. 설치 가이드 desktop과 연결 mobile 화면을 이미지로 확인했다.
- CD build·test typecheck 통과. central view 6건, history 2건, client contract 22건 통과. 실제 HTML renderer로 연결·원문 화면을 생성해 Chrome에서 표시하고 연결 화면을 이미지로 확인했다. VS Code API는 mock이며 새 Extension Host 실행의 증거는 아니다.
- 변경 문서의 로컬 파일 링크 100개와 앱 경로 링크 9개를 대조했다. CD 공개 command title 8개가 manifest와 일치한다. Helm 문서의 version 추출 명령은 실제 checkout에서 appVersion `0.8.0-alpha.63`, chart `0.10.59`를 반환했다. 설치·publish 명령 자체는 실행하지 않았다.
- GCR build의 기존 500 kB 초과 chunk 경고는 남아 있다. 실모델 호출과 운영 API 변경은 0회다.

## Commit과 적용 상태

| 대상 | Commit / 상태 |
| --- | --- |
| GCR 문서 | `124d1dc` |
| GCR 화면 source | `0bb8a2225181db1b89c5fe316b10e386402d27d1` |
| CD 문서 | `59035f8` |
| CD 화면 source | `bcaadf8` |
| CD 생성 bundle | `708710178ae1c47cd89e822d7d982fe437a8e5fd` |
| 공통 client / private service | alpha.42 / alpha.37, 변경 없음 |
| 설치 VSIX | G04의 2.11.3 유지. 이번 안내 변경은 설치하지 않음 |
| GCR 배포 | G04의 alpha.63 / chart 0.10.59 / Helm revision 73 유지. 이번 화면은 배포하지 않음 |

GCR branch는 `codex/review-memory-pull-g01`, CD branch는 `codex/review-memory-pull-g03`이다. 이번 변경은 소스와 로컬 빌드까지다. 이미 열린 사용자 Host와 PRISM-DEV에 새 안내가 적용됐다고 간주하지 않는다. 후속 전달 시 CD는 새 버전 VSIX를 만들고 GCR는 새 image에 문서 정적 asset을 포함해야 한다. 기존 2.11.3 VSIX나 alpha.63 image tag를 덮어쓰지 않는다. Marketplace 게시·강제 reload·전역 CLI 교체는 하지 않았다.

실제 기능·모델 검증은 기존 [G04 기록](../review-memory-pull/G04.md)을 재사용한다. 이 문서 작업의 fixture 검사로 그 검증 범위를 확대하지 않는다.
