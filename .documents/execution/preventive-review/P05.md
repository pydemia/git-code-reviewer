# P05 — 중앙 리뷰 기준 관리

2026-09-13 사용자 요청으로 주요 기능을 우선하도록 순서를 조정했다. 전체 P00–P13 goal은 유지한다. 시작 GCR HEAD는 `efc4bd7`, branch는 `feat/preventive-review-platform`이다.

## 초기 작업 기록

P05-C01–C03 일부를 로컬에서 구현 중이며 아직 commit·배포하지 않았다. 신규 `review-criteria` 계약·migration 0037·service·routes와 `/review-criteria` 화면을 작성했다. 후보 등록, 공용 출처 hash 고정, 독립된 decision, 불변 revision, 수동 4종 평가, repository maintainer 위임, 고위험 owner 승인, 관찰·활성·퇴역 전이와 이력을 연결했다. 개인 memory는 공용 출처로 받지 않으며 기존 memory 상태는 수정하지 않는다.

검증 초안은 `apps/runtime/src/routes/review-criteria.integration.test.ts`다. 실제 PostgreSQL에서 13개 중 11개가 통과했고, 동시 revision 변경의 잘못된 404와 Chrome 편집 전환 테스트의 대기 실패를 확인했다. 잠금은 stable rule row를 먼저 잠그도록 수정했고 detail 읽기도 하나의 snapshot으로 묶었다. 수정 후 syntax/typecheck를 통과했지만 전체 재검증은 남아 있다. Chrome 실패를 해결됐다고 기록하지 않는다. 첫 시도에는 Vite의 workspace resolve 오류도 있어 web workspace의 테스트 helper로 분리했다. 결과 원본은 ignored `artifacts/operations/P05-criteria/tests-attempt*.json`에 있다.

2026-09-14 사용자가 실제 PRISM-DEV namespace의 Keycloak Helm 배포와 DB TLS를 요청해 해당 운영 작업을 먼저 수행했다. P05 초안은 현재 checkout에 보존했으며 운영 image에는 포함하지 않았다. 다음 작업은 P05의 실패 재검증과 화면·사용성 확인을 마친 뒤 검토 가능한 commit으로 전달하는 것이다. 모델 후보 자동 추출·실제 모델 평가, 기간·범위 예외 UI, 배포 bundle·client sync는 아직 구현하지 않았다. 수동 평가 기록을 모델 실행이나 실제 결함 재현 증거로 해석하지 않는다.

## 2026-09-14 — 기준 관리 API·화면 검증

DB TLS 배포 이후 주요 기능 구현을 재개했다. 동시 수정의 409 충돌 처리와 편집 화면을 재검증하고 일반 사용자의 정정·범위/기간 예외 요청, 독립된 owner 승인, 철회·만료·revision 경계와 이력 보존을 추가했다. 직접·그룹 repository grant 모두 역할 위임과 고위험 승인에 반영한다. 저장소 삭제 시 이력 FK 충돌도 수정했다.

기준 관리 17개와 기존 메모리 9개, 총 26개 검사가 통과했다. 실제 PostgreSQL과 Chrome으로 수동 후보→평가→승인→정정/예외→revision→퇴역을 수행했고 전체 build·변경 TS/TSX lint도 통과했다. 화면의 평가 입력은 수동 관찰 기록이다. 합성 fixture의 성공을 실제 모델 실행이나 SAML 로그인 검증으로 계산하지 않는다.

P05-C01의 계약·DB와 C02/C03의 수동 관리 경로를 함께 전달한다. C02의 모델 후보 자동 생성, 역할 위임 UI, C04–C07의 발행·서명·배포 API·상태는 남아 있다. 현재 화면은 미발행 상태를 표시하며 새 기준을 기존 PR·CLI 리뷰에 적용했다고 주장하지 않는다. [기능·API 기록](../../../docs/operations/review-criteria-management-2026-09-14.md)과 [검증 증거](evidence/P05-criteria-management.json)를 따른다. 이 checkpoint에서는 배포를 완료한 것으로 기록하지 않는다.


## PRISM-DEV 전달

| 작업 | SHA·전달 상태 |
| --- | --- |
| P05-C01 계약·DB, C02-a 수동 관리, C03-a 기준/정정/예외 UI | `b4ca4cc`, push 완료 |
| Image/chart pin | `4230448`, push 완료 |
| 운영 배포 | alpha.39, chart 0.10.37, Helm revision 51, migration 37개 |

2026-09-14 01:11 KST 실제 PRISM-DEV 검증을 완료했다. 고정 image digest는 `sha256:f8477a2b9f3513ddba25442d34a02abb51837280369d2811f85969663b9f49a2`다. 게시된 amd64 image에서 36→37 migration, 기존 fixture 사용자 보존, 전체 production server의 실제 local 로그인·인증/Origin 검사·기준 등록·수정·stale 충돌을 통과했다. SBOM/provenance와 OCI chart의 bytes도 대조했다.

운영 server·worker의 compiled 코드와 웹 asset이 해당 image와 일치하고 Helm test가 01:11:03 KST에 통과했다. 운영 DB의 기존 사용자 7명·chat 계정 7개·분석 157건·report 149건·grant 2개·membership 7개·memory 4개·local credential 6개, 기존 36개 migration checksum, Secret·ConfigMap·route·PVC와 image 외 설정을 보존했다. DB `verify-full`, local 인증과 별도 Keycloak 2/2를 유지했다. 인증된 전체 기준 workflow는 동일 image의 격리 fixture에서 검증했으며 운영 사용자 계정으로 기준을 생성한 검증으로 확대하지 않는다.

[실제 배포 증거](evidence/P05-PRISM-deployment.json)를 따른다. 다음 구현은 모델 후보 생성·역할 위임 화면과 P05-C04–C07의 발행·manifest/API다. P05 전체와 P00–P13 goal은 계속 진행 중이다.


## 모델 후보 생성·역할 위임 구현

P05-C02-b/C03-b로 등록 모델 계정 선택, 영속 후보 생성 큐, worker 처리와 상태/취소 UI, 관리자 역할 위임 화면을 구현했다. Migration 0038은 실행 요청을 저장하며 기존 0037은 수정하지 않았다. 모델 출력은 평가·승인 전 draft로만 등록한다. 권한·원문을 재확인하고 실행 결과가 불명확하면 자동 재호출하지 않는다.

Node 22에서 기준 관리 21개와 기존 메모리 9개, 총 30개 검사가 통과했다. 실제 PostgreSQL·Chrome과 합성 provider를 사용해 생성·조회·역할 위임/회수 및 기존 전체 기준 workflow를 확인했다. 첫 검증의 source 오류 코드와 이전 후보를 선택한 브라우저 검사를 수정했고 역할 저장 중 checkbox 상태도 보완했다. 최종 전체/runtime/web build와 변경 TS/TSX lint가 통과했다. 상세 증거는 [P05-criterion-generation.json](evidence/P05-criterion-generation.json)이다. 실제 provider 호출·새 image 검증·운영 배포는 다음 전달 단계에서 확인한다. C04–C07의 발행·서명·배포 API·상태와 CD 연동은 남아 있다.


## PRISM-DEV 모델 후보 생성 전달

`19ebf42`에 P05-C02-b/C03-b를 commit·push했고 `1366f06`에 image/chart pin을 전달했다. alpha.40·chart 0.10.38을 Helm revision 52로 배포했다. Image digest는 `sha256:875b28b201b1aeb72b1a10f1f99ad675b0c5e8b89d9074e1342b19751ea30e76`이며 migration 38개, server·worker Ready, 기존 37개 migration checksum과 image 외 설정·기존 데이터 보존을 확인했다. 게시된 amd64 image의 전체 서버 로그인·기준 API·생성 큐, SBOM/provenance, OCI chart bytes를 확인했고 운영 compiled 코드·웹 asset도 대조했다. Helm test는 2026-09-14 01:45:03 KST에 통과했다.

실제 운영 API와 worker에서 등록된 `gpt-5.6-sol / medium`으로 명시적 합성 캐시 검토 기록 한 건을 처리했다. 모델 admission ledger는 완료 호출 1건이며 중복 접수는 같은 요청 ID를 반환했다. 모델은 적용 조건·반증·검토 절차를 생성하고 실제 코드·실행 결과가 없다는 근거로 `open-question`을 선택했다. 저장 결과는 `draft`, 평가 0개, 원문과 hash가 동일했다. 최초 bootstrap 로그인 시도는 401로 거부돼 실제 모델 호출로 계산하지 않았다. 기존 credential을 바꾸지 않고 임시 reviewer·해당 검증 repository의 maintainer 역할과 실제 local 로그인을 사용했다. 완료 후 검증 repository·사용자·세션을 정리했다.

[실제 배포·모델 증거](evidence/P05-generation-PRISM-deployment.json)를 따른다. 이제 후보 관리 경로의 다음 작업은 P05-C04–C07의 승인된 자료 projection·outbox·불변 bundle, 서명된 manifest와 배포 API다. P06의 Commit Defender·CLI sync와 P00–P13의 나머지 범위는 계속 진행한다.

## P05-C04 — 불변 bundle·outbox 로컬 구현

Migration 0039에 repository/component/owner scope, 배포 projection 승인, outbox, 불변 release와 현재 pointer를 추가했다. 기준·메모리·원문·Skill·권한 변경의 transaction에서 요청 revision을 기록한다. Worker는 snapshot과 실행권을 대조하고 파일 저장·hash readback 후에만 pointer를 변경한다. Artifact registry와 기존 retention 잠금을 재사용한다.

공통 client-contract에 2 MiB 완전 bundle과 canonical JSON 계약을 추가했고 runtime·Docker 의존성에 연결했다. 정책에는 실제 Skill 본문과 유효한 active 기준을, 메모리에는 별도 승인한 배포 내용과 제한된 출처 참조를 담는다. Source 내용/공개 범위 변경과 사용자 제외를 검증하고 개인 owner를 분리한다. Projection 자체의 hash와 원래 sourceContentHash를 구분한다.

발행 검사 10개와 기존 기준·메모리·공통 계약 검사 93개가 통과했다. 전체 build·후속 client-contract/runtime build, 변경 TS lint와 Helm lint도 통과했다. 출처 참조를 추가한 최종 변경은 발행 검사 10개로 다시 확인했다. 사용자 제외 후 null fingerprint를 DB 제약 오류로 전달하던 경로는 명시적 409 거부로 수정했다. [증거](evidence/P05-knowledge-publication.json)와 [운영·구현 문서](../../../docs/operations/review-knowledge-publication.md)를 따른다.

이 변경은 아직 운영 배포하지 않았다. PRISM-DEV는 alpha.40/revision 52/migration 38을 유지한다. 다음은 P05-C05의 서명된 coherent manifest·lease·rollback, C06의 인가된 다운로드·projection 승인 API, C07의 발행 상태 화면이다. 이 경로를 연결해 배포한 뒤 P06의 Commit Defender·CLI 원자적 sync와 실제 리뷰를 검증한다. 전체 P00–P13 goal은 진행 중이다.


## 2026-09-14 — 서명된 manifest와 지식 배포 API

C04의 세 component 발행에 C05/C06의 서명·배포 경계를 연결했다. Manifest는 사용자·저장소 범위, 현재 DB 권한 revision, 세 component의 release/hash/크기와 offline lease를 Ed25519로 서명한다. 조회마다 현재 권한과 파일 무결성을 확인한 후에만 ETag 304를 반환한다. Download는 요청자 소유 snapshot과 현재의 완전한 release 조합을 검증한다. Client-core에 사전 신뢰 공개키·audience·서명·만료·권한/sequence 하한 검증 함수를 추가했다.

공용 메모리는 maintainer, 개인 메모리는 본인만 projection을 조회·승인할 수 있도록 API를 연결했다. 원문 fingerprint가 바뀌면 이전 승인 요청을 거부한다. 빈 membership upsert 같은 실제 변경 없는 identity 갱신이 모든 bundle과 manifest를 무효화하지 않도록 migration 0040에서 관련 trigger를 실제 변경 row 기준으로 바꿨다. 살아 있는 manifest는 불변이며 worker는 만료 cache만 제한된 batch로 삭제한다. Helm은 signing key를 server container에만 mount한다.

기존 기준·메모리·계약·서명·설정과 배포 API를 합친 141개 테스트가 통과했다. 이후 실제 개발 인증 hook과 local password 로그인·logout을 추가한 최종 배포 API 15개도 통과했다. 실제 PostgreSQL·filesystem·Chrome을 사용했으며 외부 모델은 호출하지 않았다. 전체 build·변경 TypeScript lint·Helm lint와 서버 전용 키 mount render 검증도 통과했다. 초기 테스트의 잘못된 모듈 경로와 logout 응답 기대값(200→204)을 수정했으며 최종 결과와 실패 시도는 각각 보존했다.

[운영·API 계약](../../../docs/operations/review-knowledge-distribution.md)과 [검증 증거](evidence/P05-knowledge-distribution.json)를 따른다. C05 전체 완료로 계산하지 않는다. 명시적 rollback을 높은 sequence로 재발행하는 경로, 출처 상세 API, C07 화면, P04 client 인증과 P06 cache·동기화는 남아 있다. 이 checkpoint는 local source commit이며 PRISM-DEV는 alpha.40·migration 38이다. 0039/0040·새 image·signing Secret은 아직 운영에 적용하지 않았다. 전체 P00–P13 goal은 유지한다.


## 2026-09-14 — 발행 상태·메모리 배포 승인 화면

C07의 저장소별 세 component 상태·버전·오류·제외 수와 C06 projection 승인 화면을 연결했다. 지원 contract는 v1, 실제 sync 관측은 unknown으로 표시한다. 배포가 꺼져 있어도 상태는 조회하며 관리 가능한 메모리는 본인 개인 자료 또는 위임된 공용 자료만 페이지별로 조회한다. 원문 변경 409는 입력을 보존하고 최신 상태 재조회는 명시적으로 입력을 버리는 작업으로 구분했다.

관련 suite 118개가 통과했고 후속 화면 재검증에서 최신 상태 요청과 기존 form 입력 사이의 경합을 발견해 수정했다. 수정 후 배포 API·Chrome 18개가 통과했다. 데스크톱/모바일 screenshot을 직접 확인했으며 외부 모델은 호출하지 않았다. 전체 build, 최종 web build와 변경 코드 lint를 통과했다. [운영 문서](../../../docs/operations/review-knowledge-management.md)와 [검증 증거](evidence/P05-knowledge-management.json)를 따른다.

서명·발행·화면을 포함한 다음 image/Helm 배포를 준비한다. 이 source checkpoint 시점에는 PRISM-DEV alpha.40·migration 38이 유지된다. C05 rollback, C06 remote resolve·출처 상세, C07 실제 client sync 보고와 P04/P06 인증·cache·동기화는 남아 있다. 전체 P00–P13 goal을 유지한다.
