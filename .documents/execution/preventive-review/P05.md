# P05 — 중앙 리뷰 기준 관리

2026-09-13 사용자 요청으로 주요 기능을 우선하도록 순서를 조정했다. 전체 P00–P13 goal은 유지한다. 시작 GCR HEAD는 `efc4bd7`, branch는 `feat/preventive-review-platform`이다.

## 현재 작업

P05-C01–C03 일부를 로컬에서 구현 중이며 아직 commit·배포하지 않았다. 신규 `review-criteria` 계약·migration 0037·service·routes와 `/review-criteria` 화면을 작성했다. 후보 등록, 공용 출처 hash 고정, 독립된 decision, 불변 revision, 수동 4종 평가, repository maintainer 위임, 고위험 owner 승인, 관찰·활성·퇴역 전이와 이력을 연결했다. 개인 memory는 공용 출처로 받지 않으며 기존 memory 상태는 수정하지 않는다.

검증 초안은 `apps/runtime/src/routes/review-criteria.integration.test.ts`다. 실제 PostgreSQL에서 13개 중 11개가 통과했고, 동시 revision 변경의 잘못된 404와 Chrome 편집 전환 테스트의 대기 실패를 확인했다. 잠금은 stable rule row를 먼저 잠그도록 수정했고 detail 읽기도 하나의 snapshot으로 묶었다. 수정 후 syntax/typecheck를 통과했지만 전체 재검증은 남아 있다. Chrome 실패를 해결됐다고 기록하지 않는다. 첫 시도에는 Vite의 workspace resolve 오류도 있어 web workspace의 테스트 helper로 분리했다. 결과 원본은 ignored `artifacts/operations/P05-criteria/tests-attempt*.json`에 있다.

2026-09-14 사용자가 실제 PRISM-DEV namespace의 Keycloak Helm 배포와 DB TLS를 요청해 해당 운영 작업을 먼저 수행했다. P05 초안은 현재 checkout에 보존했으며 운영 image에는 포함하지 않았다. 다음 작업은 P05의 실패 재검증과 화면·사용성 확인을 마친 뒤 검토 가능한 commit으로 전달하는 것이다. 모델 후보 자동 추출·실제 모델 평가, 기간·범위 예외 UI, 배포 bundle·client sync는 아직 구현하지 않았다. 수동 평가 기록을 모델 실행이나 실제 결함 재현 증거로 해석하지 않는다.

## 2026-09-14 — 기준 관리 API·화면 검증

DB TLS 배포 이후 주요 기능 구현을 재개했다. 동시 수정의 409 충돌 처리와 편집 화면을 재검증하고 일반 사용자의 정정·범위/기간 예외 요청, 독립된 owner 승인, 철회·만료·revision 경계와 이력 보존을 추가했다. 직접·그룹 repository grant 모두 역할 위임과 고위험 승인에 반영한다. 저장소 삭제 시 이력 FK 충돌도 수정했다.

기준 관리 17개와 기존 메모리 9개, 총 26개 검사가 통과했다. 실제 PostgreSQL과 Chrome으로 수동 후보→평가→승인→정정/예외→revision→퇴역을 수행했고 전체 build·변경 TS/TSX lint도 통과했다. 화면의 평가 입력은 수동 관찰 기록이다. 합성 fixture의 성공을 실제 모델 실행이나 SAML 로그인 검증으로 계산하지 않는다.

P05-C01의 계약·DB와 C02/C03의 수동 관리 경로를 함께 전달한다. C02의 모델 후보 자동 생성, 역할 위임 UI, C04–C07의 발행·서명·배포 API·상태는 남아 있다. 현재 화면은 미발행 상태를 표시하며 새 기준을 기존 PR·CLI 리뷰에 적용했다고 주장하지 않는다. [기능·API 기록](../../../docs/operations/review-criteria-management-2026-09-14.md)과 [검증 증거](evidence/P05-criteria-management.json)를 따른다. 이 checkpoint에서는 배포를 완료한 것으로 기록하지 않는다.


## PRISM-DEV 전달

| 작업 | SHA·전달 상태 |
| --- | --- |
| P05-C01 계약·DB, C02-a 수동 관리, C03-a 기준/정정/예외 UI | `b4ca4cc`, push 완료 |
| Image/chart pin | `4230448`, push 완료 |
| 운영 배포 | alpha.39, chart 0.10.37, Helm revision 51, migration 37개 |

2026-09-14 01:11 KST 실제 PRISM-DEV 검증을 완료했다. 고정 image digest는 `sha256:f8477a2b9f3513ddba25442d34a02abb51837280369d2811f85969663b9f49a2`다. 게시된 amd64 image에서 36→37 migration, 기존 fixture 사용자 보존, 전체 production server의 실제 local 로그인·인증/Origin 검사·기준 등록·수정·stale 충돌을 통과했다. SBOM/provenance와 OCI chart의 bytes도 대조했다.

운영 server·worker의 compiled 코드와 웹 asset이 해당 image와 일치하고 Helm test가 01:11:03 KST에 통과했다. 운영 DB의 기존 사용자 7명·chat 계정 7개·분석 157건·report 149건·grant 2개·membership 7개·memory 4개·local credential 6개, 기존 36개 migration checksum, Secret·ConfigMap·route·PVC와 image 외 설정을 보존했다. DB `verify-full`, local 인증과 별도 Keycloak 2/2를 유지했다. 인증된 전체 기준 workflow는 동일 image의 격리 fixture에서 검증했으며 운영 사용자 계정으로 기준을 생성한 검증으로 확대하지 않는다.

[실제 배포 증거](evidence/P05-PRISM-deployment.json)를 따른다. 다음 구현은 모델 후보 생성·역할 위임 화면과 P05-C04–C07의 발행·manifest/API다. P05 전체와 P00–P13 goal은 계속 진행 중이다.
