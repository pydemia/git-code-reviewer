# P12 — 리뷰 이력과 지속 학습

## 2026-09-15 — PR 원문·리뷰 상태·위치 이력

P12-C01의 REST 원천 보존 경로를 구현하고 배포했다. 기존 수집에서 버리던 본문 없는 제출된 승인/기각 리뷰를 보관하고 공백을 포함한 원문을 유지한다. 현재 줄이 없을 때 원래 줄을 대신 넣지 않으며 review state·review ID·답글 대상·원래 commit/줄 범위·diff hunk를 별도 provenance로 저장한다. REST에 없는 thread resolved/outdated 상태는 unknown으로 남긴다.

Migration 0046은 body-only 버전 테이블을 보존하고 immutable 관측 이력을 추가한다. 상태만 바뀌어도 새 이력이 생기며 같은 snapshot의 반복 수집은 이력이나 knowledge outbox를 늘리지 않는다. A→B→A 복귀는 별도 이력이다. PR별 저장 직렬화, 수집 시작 시각과 provider 수정 시각 비교로 늦은 응답이 최신 자료를 덮어쓰지 못하게 했다. 같은 시작 시각의 서로 다른 응답도 보수적으로 거부한다.

중앙 PR 대화에서 현재/원래 위치, 리뷰 상태, 답글 대상과 이력을 확인한다. 이력 GET은 현재 저장소 권한과 source/PR 소속을 검사하고 50건 단위 cursor를 제공한다. 원문과 diff는 text로 표시하며 빈 본문으로 Memory 후보를 만들지 않는다. 중앙 웹 조회 기능이며 local upload나 중앙 executor를 추가하지 않았다.

기준 원천에 observation hash와 실제 discussion metadata를 고정한다. 원문 선택 후 상태·위치가 바뀌면 오래된 hash로 후보를 생성할 수 없다. 변경된 출처를 참조한 기준·memory projection은 다음 발행에서 제외하며 재검토·승인이 필요하다. 이전 bundle은 수정하지 않는다. 기존 자료의 첫 metadata 관측도 출처 변경으로 취급하므로 승인된 항목의 재검토가 필요할 수 있다.

최종 7개 suite 81건, 실제 PostgreSQL 18.3 fixture와 cleanup, contracts/GitHub/runtime/web build, 변경 파일 ESLint와 Helm lint가 통과했다. Chrome에서는 키보드 펼침·이전/최근 페이지·권한 오류 후 이력 제거·원문 HTML 비실행과 1100/420px 화면을 검증하고 screenshot을 확인했다. 별도 45→46 업그레이드 시험에서 기존 댓글·본문 버전·45개 migration checksum 보존, 관측 이력 소급 생성 없음과 재실행 idempotence를 확인했다.

Source `3f220cf9f3c6da4be4ed6e3cbb9a40bb557f3a1d`, pin `8bdf3b9`의 app `0.8.0-alpha.51`/chart `0.10.47`을 PRISM-DEV Helm revision 61로 배포했다. Image digest는 `sha256:fd13277943a8ea8a21934108987f2ea5593d529521c7245bb9840d461fda2fb2`다. Registry digest/source label·chart bytes, nonroot/read-only 이미지의 파일·정적 경로·migration, server dry-run과 실제 server/worker compiled hash·HTTPS asset·readiness를 대조했다. Helm test는 02:27:57 KST에 통과했다.

실제 배포 코드에 합성 REST 응답을 주어 수집·이력 저장을 실행하고 HTTPS 로그인 session으로 현재 원천과 두 상태 이력을 조회했다. 빈 승인, 원래 줄 분리, 늦은 응답 차단, 다른 PR/미인증 조회 거부와 저장소 권한 철회 후 404를 확인했다. 외부 GitHub·모델은 호출하지 않았다. 시험용 repository/user와 관측 이력을 cascade 정리하고 잔여 0건을 확인했다.

배포 후 schema 46, 기존 데이터·migration checksum·Secret·HTTPRoute·PVC·image 외 Helm 설정과 DB verify-full을 보존했다. Server/worker와 Keycloak 2개 replica는 ready이며 현재 인증 모드는 local이다. 임시 BuildKit·smoke 리소스도 정리했다. Commit Defender 2.9.3과 client alpha36/CLI alpha32는 기존 단방향 수신 경로를 유지한다.

GraphQL thread 상태, 명시적 삭제/접근 불가 관측, 최근 종료 PR 회수·증분/backfill, 여러 논의와 수정의 판단 연결·재검토 UI는 남아 있다. P12 전체나 전체 goal 완료로 판정하지 않는다. P10 및 P08의 로컬 제출 기능은 철회 범위다. [검증 증거](evidence/P12-review-provenance.json)와 [운영 계약](../../../docs/operations/pr-review-provenance.md)을 참고한다.

## 2026-09-15 — 스레드 상태와 PR 대화 재수집

P12-C01의 GraphQL 스레드 상태와 P12-C02의 대화 재시도 경로를 배포했다. REST 댓글 node ID를 GraphQL comment ID와 연결해 thread ID·resolved·outdated를 관측한다. 기존 GitHub credential로 같은 origin의 읽기 query만 호출하며 API 미지원·오류·부분 응답·cursor 반복·페이지 간 상태나 PR 범위 변경은 unknown으로 남긴다. 스레드가 해결됐다는 표시를 코드 결함 수정으로 판정하지 않는다.

Migration 0047은 PR 목록 ETag와 별도로 대화 수집 예정 시각·실패·실행권을 저장한다. 목록 응답이 304여도 예정된 대화를 읽는다. 저장소 poll당 최대 10개 PR을 처리하며 한 PR의 실패가 다른 PR 수집이나 metadata 저장을 되돌리지 않는다. 실행권은 2분 후 만료되고 이전 소유자의 저장·성공 처리는 거부한다. 실패 재시도 간격은 30초부터 최대 30분이다.

추적 중인 open PR은 종료 관측 후 7일 동안 후속 논의를 읽고 reopen하면 계속 추적한다. Migration은 기존 open PR과 최근 갱신됐으며 이미 원문이 있는 closed PR만 등록한다. 처음 발견한 과거 closed PR 전체의 대화를 자동 수집하지 않는다. REST는 60초·endpoint당 20페이지, GraphQL은 전체 20초·20회·응답당 2 MiB로 제한한다. REST 페이지 한도에 도달하면 실패로 남기고, GraphQL 부분 응답은 스레드 미확인으로 표시한다. PR 대화 화면은 원문 수집 성공·실패·재시도와 개별 스레드 관측 상태를 구분한다.

최종 회귀 9개 suite 94건, contracts/GitHub/runtime/web build, 변경 파일 ESLint와 Helm lint가 통과했다. PostgreSQL 18.3에서 metadata 304 후 갱신, 종료 직후 실패의 재시도, 23개 PR의 10/10/3 순환, 동시 claim·만료 복구·이전 소유자 거부를 확인했다. 별도 46→47 시험은 기존 본문 버전·46개 checksum 보존, 소급 관측 생성 없음, 대상 PR만 queue 등록과 재실행 idempotence를 검증했다. 첫 업그레이드 검증은 합성 PR 매개변수의 bigint/integer 타입 충돌로 중단됐고 검증 스크립트의 명시적 cast를 수정한 뒤 통과했다.

Chrome에서 키보드 이력 펼침·페이지 전환·권한 오류 후 이전 자료 제거·원문 HTML 비실행을 확인했다. 현재 resolved/outdated 관측과 과거 미확인 사유를 함께 표시하며 1100/420px overflow 검사와 screenshot 확인을 마쳤다.

Source `3ca30f05c571a41f5c5769d2d0a05726fa3ab81f`, pin `ba3c1c7`의 app `0.8.0-alpha.52`/chart `0.10.48`을 PRISM-DEV Helm revision 62로 배포했다. Image digest는 `sha256:00c525ff881b56f35631a18d3b8f12c00fbb019c41bb60e4411c44bed34bf589`다. Registry source/digest·chart readback·격리 이미지 검사와 server dry-run을 통과했다. Helm 연결 검사는 02:53:49 KST에 성공했다. 배포 후 schema 47, server/worker의 compiled hash와 HTTPS assets·readiness를 대조했으며 기존 데이터·migration checksum·Secret·HTTPRoute·PVC·image 외 설정과 DB verify-full을 보존했다. Server/worker와 Keycloak 2개 replica가 ready이며 인증 모드는 local이다.

실제 배포 모듈에 합성 REST/GraphQL 응답을 주어 관측·저장·304 대화 재수집을 실행하고 HTTPS 로그인 session으로 최신 원천과 상태 이력을 조회했다. 다른 PR·미인증·권한 철회 후 접근 거부를 확인했다. 처음 두 시도는 합성 GraphQL 응답의 괄호 누락으로 수집 전에 중단됐으며 시험 데이터를 각각 정리했다. 검증 스크립트를 수정하고 내장 프로그램 구문 검사를 추가한 뒤 최종 검사가 통과했다. 외부 GitHub·모델은 호출하지 않았다. 시험 repository/user와 임시 BuildKit·smoke 리소스 잔여가 없음을 확인했다.

중앙에서 발행한 리뷰·지식·프롬프트를 로컬이 내려받는 단방향 구조를 유지한다. Commit Defender 2.9.3과 client alpha36/CLI alpha32는 변경하지 않았으며 로컬 모델·계정 설정, source/결과/피드백 업로드나 중앙 executor를 추가하지 않았다.

명시적 삭제/접근 불가 관측, cursor를 저장해 재개하는 대규모 수집, 범위 지정 backfill·quota/webhook 보정, 여러 논의와 코드 수정의 판단 연결·재검토 UI는 남아 있다. P12와 전체 goal은 진행 중이며 P10 및 P08 upstream 제출은 철회 범위다. [검증 증거](evidence/P12-thread-sync.json)에 이번 배포의 근거와 한계를 기록했다.

## 2026-09-15 — 코드 변경과 리뷰 판단의 출처 연결

새 중앙 Git snapshot의 파일 diff를 PR 논의·집단 메모리·수동 기록과 함께 기준 후보에 연결한다. 원문과 snapshot ID·PR 번호·경로·변경 종류·base/merge-base/head SHA를 hash로 고정한다. 변경 전 기준은 merge-base이며 PR의 현재 head가 바뀌어도 과거 판단의 출처는 유지한다. 모델 후보에는 연결된 코드와 반대 논의를 함께 제공하고, 다른 endpoint의 지적을 주제 유사도만으로 합치거나 diff·merge·resolve를 수정 검증으로 해석하지 않도록 지시한다. 결과는 기존 평가·승인 절차를 거치는 draft다.

Migration 0048은 새 exact snapshot의 12,000자 이하 텍스트 diff를 `snapshot_change_sources`에 보관한다. Binary·빈 diff·unresolved·큰 diff·이전 snapshot의 출처를 잘라 만들거나 소급 생성하지 않는다. 기존 artifact는 보존한다. 생성 전후와 발행 시 repository 소속과 원문/metadata hash를 다시 확인한다. 출처가 사라진 기준은 다음 발행에서 제외하며 이전 bundle은 보존한다. 화면에서 선택 전에 전체 diff와 수정 전후 SHA를 확인할 수 있다.

발행되는 자료는 승인된 기준과 출처 kind·ID·hash다. 중앙 diff 원문·코드 metadata·논의는 bundle에 넣지 않는다. 공용 클라이언트의 기존 enum이 새 출처를 거부하는 것을 발행 검사에서 발견해 지식 계약 v3과 client alpha37/CLI alpha33을 함께 갱신했다. 서버는 실제 bundle에 코드 출처가 있는지 검사해 최소 버전을 서명한다. 해당 발행물에 대한 구버전 manifest 요청은 cached recipe가 있어도 HTTP 426이며, 새 클라이언트는 v2 자료도 읽는다. 이전 서버의 명시적 HTTP 426에만 같은 서버의 v2를 한 번 요청하며 두 버전 모두 거절되면 중단한다.

GCR의 현재 회귀 247건을 검증했다. 10개 suite 실행에서 246건이 통과했고 기존 HTTP 426 무재요청 기대값 하나가 실패했다. 계약 협상에 맞춰 v3/v2 두 요청 이후 중단을 검증하도록 교체한 뒤 해당 HTTP suite 38건이 모두 통과했다. 실제 PostgreSQL에서 snapshot artifact와 출처의 일치, PR SHA 갱신 후 과거 자료 유지, 다른 저장소·변경된 hash 거부, 삭제 후 생성 중단과 다음 발행의 제외를 확인했다. 별도 47→48 업그레이드는 기존 snapshot·대화 queue·댓글·본문 버전·47개 checksum 보존, 출처 소급 생성 없음과 idempotence를 검증했다. 최초 추가 fixture의 필수 github_updated_at 누락은 수정한 뒤 다시 검증했다.

Contracts/client/runtime/web/CLI build, 변경 파일 ESLint와 Helm lint를 통과했다. Chrome에서 실제 기준 API로 출처 선택·후보 생성을 실행하고 diff 원문 일치·HTML 비실행·1360/420px overflow와 screenshot을 확인했다. Node 22의 clean consumer와 CLI 설치 실행, Node 18의 실제 tarball import를 검증했다.

Commit Defender source `1219342ebacb9e62ca64152b305ecfd6529a24f7`의 2.9.4를 설치했다. 확장 회귀 190건과 source/test typecheck·build가 통과했다. 새 v3 기준을 자체 HTTPS fixture에서 가져와 로컬 합성 실행기에 적용하고 로컬 이력을 복원했다. 이 검사에서 중앙 요청은 모두 GET이고 결과 제출은 0회였다. VSIX를 VS Code 1.90.2/1.135.0에서 활성화해 25개 명령·기본 자동 실행 꺼짐·중앙 executor/제출 명령 부재를 확인했다. 설치된 38개 파일·package metadata·등록 정보가 VSIX와 일치한다. VSIX SHA-256은 `0ff5fe77f98537a404f80844d39e9bc8b073553a5c753c2f790a9345e9177ee7`이며 사용자 창 reload와 전역 CLI 교체는 하지 않았다.

GCR source `6cd8f53423c2d913c63f9c5b8d1264d1a62103c2`, pin `bfa273d`의 app 0.8.0-alpha.53/chart 0.10.49를 PRISM-DEV Helm revision 63으로 배포했다. Image digest는 `sha256:b8849ca59f020fe12b63a91979d96932d5a58ef230ad4b6e1038e703a8e63879`다. Registry와 OCI chart readback, 격리 이미지 검사, server dry-run을 통과했다. Helm 연결 검사는 03:26:50 KST에 성공했다. Schema 48과 server/worker의 compiled hash·HTTPS assets·readiness를 대조했으며 기존 데이터·migration checksum·Secret·HTTPRoute·PVC·image 외 설정·DB verify-full을 보존했다. Server/worker와 Keycloak 2개 replica가 ready이고 인증 모드는 local이다.

실제 배포 모듈에 합성 코드 출처를 저장한 뒤 HTTPS session으로 출처 조회·다중 출처 draft 생성·평가 전 활성화 거부·미인증/권한 철회 후 조회 거부를 확인했다. 외부 GitHub나 실제 모델 계정은 호출하지 않았다. 시험 repository/user와 BuildKit·smoke 리소스를 정리했다.

전체 파일 문맥과 수정 검증을 포함한 판단 연결, 과거 snapshot의 범위 지정 회수, 삭제 즉시 재발행 요청과 재검토 UI는 남아 있다. 이번 작업으로 P12-C03 전체 또는 전체 goal 완료를 판정하지 않는다. 중앙→로컬 단방향 전파와 로컬 모델 실행을 유지한다. [검증 증거](evidence/P12-code-decisions.json)와 [운영 계약](../../../docs/operations/review-decision-code-sources.md)에 범위와 한계를 기록했다.

## 2026-09-15 — 출처 변경·예외 만료 재검토

P12-C04의 재검토 조회·평가/승인 차단·새 버전 작성 흐름과 발행 일정을 구현했다. 현재 출처의 원문·metadata가 고정된 판단과 다르면 기존 버전의 평가·승인을 거부하고, 사용할 수 없는 출처는 상세 응답의 과거 버전에서도 원문과 metadata를 가린다. DB의 판단·평가·승인 이력은 보존한다. 퇴역은 가능하며 새 버전은 최신 출처 선택과 평가·승인을 다시 거친다.

Migration 0049는 코드 출처·snapshot 변경/삭제 시 같은 transaction에서 발행을 요청하며 재검토 날짜와 예외 시작·만료를 queue에 저장한다. 발행 worker는 약 1분마다 최대 100건을 처리한다. 재검토 날짜만으로 기준을 제외하지 않고 만료한 예외만 새 bundle에서 제거한다. 과거 bundle은 보존한다.

회귀 6개 suite 123건, contracts/runtime/web build, 변경 파일 ESLint와 Helm lint가 통과했다. PostgreSQL 18.3에서 변경·삭제 후 평가/승인 거부, 원문 가림과 이력 보존, 새 버전 재평가, parent 삭제의 실제 실행·재발행 요청, 무변경 UPDATE의 미요청, 동시 worker의 일정 중복 처리 방지, 퇴역/구버전 일정 무시와 날짜/예외 만료 후 기준 유지를 확인했다. Chrome에서 재검토 필터·원문 가림·버튼 상태·새 출처 선택·재검토 시각 입력을 실제 API로 검증하고 1360/420px overflow와 screenshot을 확인했다. 첫 API 시험에서는 합성 memory의 개인 범위 unique key 충돌을 fixture 소유자 변경으로 수정했다.

48→49 별도 업그레이드는 기존 기준·판단·평가 관련 이력과 코드 출처, 대화 queue, 48개 checksum을 보존하고 현재 비퇴역 버전의 일정만 등록하며 재실행에 변화가 없음을 확인했다. 첫 업그레이드 시도는 합성 기준의 필수 appliesTo 누락으로 중단했고 fixture를 수정한 뒤 통과했다.

중앙→로컬 단방향 다운로드와 로컬 모델 실행을 유지한다. 이번 변경은 공용 클라이언트 bundle 계약을 변경하지 않아 Commit Defender 2.9.4/client alpha37/CLI alpha33을 유지한다. 배포 증거는 후속 기록에 연결한다. Provider의 명시적 삭제/접근 불가 관측과 critical 철회/offline 처리는 남아 있으며 P12-C04 전체와 전체 goal은 진행 중이다. [운영 계약](../../../docs/operations/review-criterion-rechecks.md)을 참고한다.

Source `270c6b29386173955f76ec987bcbe5711726b427`, pin `2226f41`의 app `0.8.0-alpha.54`/chart `0.10.50`을 PRISM-DEV Helm revision 64로 배포했다. 배포 시각은 03:55:49 KST이며 image digest는 `sha256:e25fab5538c229cf5cc433d189a4bf4849f95fea4975cfa9aea3c18d338eab10`이다. Registry source/digest·OCI chart bytes·격리 이미지 검사·server dry-run을 통과했고 Helm 연결 테스트는 03:56:40 KST에 성공했다. 빌드 종료 때 BuildKit stream의 closed-pipe 경고가 있었으나 빌드 exit 0이며 게시된 image/config digest와 파일 검증이 통과했다.

실제 배포 코드와 HTTPS session에서 합성 출처 생성·삭제, 같은 transaction의 재발행 요청, 삭제가 trigger에 의해 취소되지 않음, 상세 응답 원문/metadata 가림, 오래된 기준의 평가 통과 거부와 퇴역 가능, 미인증·권한 철회 후 조회 거부를 확인했다. 외부 GitHub와 실제 모델 계정은 호출하지 않았다. 시험 사용자·저장소와 임시 BuildKit·smoke 리소스 잔여가 없음을 확인했다.

배포 후 schema 49, 서버·worker의 compiled hash와 HTTPS assets·readiness를 대조했다. 기존 데이터·48개 migration checksum·Secret·HTTPRoute·PVC·image 외 Helm 설정과 DB verify-full을 보존했으며 서버·worker와 Keycloak 2개 replica가 ready다. 인증 모드는 local이다. [배포와 검증 증거](evidence/P12-rechecks.json)에 이번 checkpoint의 범위와 남은 작업을 기록했다.
