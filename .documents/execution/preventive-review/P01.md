# P01 실행 기록

상태: 진행 중. 2026-09-13 현재 P01-C01–C05의 구현·코드 회귀를 완료했다. 전체 개발·배포 범위는 유지하며 P01 완료나 제품 릴리스를 선언하지 않는다.

## 완료한 commit

작업 checkout은 `/Users/a09255/git/commit-defender-preventive-review`, branch는 `feat/preventive-review-platform`이다. 현재 세션에서 작업했으며 codex-work의 별도 checkout은 사용하지 않았다.

| ID | CD commit | 변경·검증 |
| --- | --- | --- |
| P01-C01 | `42cb604` | Credential·local store·QA/생성물·ignored 파일의 공통 수집 정책. 일반 untracked 포함, symlink·rename·literal 파일명, exclusion coverage와 실제 hook bundle 입력 검증. Source policy 10개·기존 provider 7개 통과 |
| P01-C02 | `e761614` | 고정한 base/index tree에서 diff·삭제·rename·marker 조회. 저장 파일도 provider 호출 전에 capture. TODO·type-ignore 면제를 prompt/후처리에서 제거. Snapshot 14개와 전체 테스트 31개 통과 |
| P01-C03 | `b3c7ed9` | 완료/부분/실패/취소와 파일별 미수행·중단 사유 보존. 기존 hook 차단과 advisory 분리. Summary를 가짜 P3 finding으로 만들지 않고 UI에 상태·coverage 반영. 신규 outcome 23개 통과 |
| P01-C04 | `2cc3dde` | Markdown AST·불투명 link ID·webview nonce/CSP, source hash와 경로/line 검증, 변경·삭제 source 읽기 전용 이동, Skill을 untrusted 자료로 분리. 기존 54개·신규 security 9개와 Node 18 security 통과 |

| P01-C05 | `21f6aa2` | ReviewBackend와 준비된 source/context, 동일 실행 합류·최신 UI 소유권·cancel/timeout/clear 처리. 전체 73개·Node 18 backend 10개 통과 |

C01–C05를 CD의 `origin/feat/preventive-review-platform`에 push했다. C02의 `npm run typecheck:test`, `npm run build`와 설치된 실제 VS Code 1.135.0 Extension Host가 exit 0으로 종료했다. Extension Host에서는 activation, command 18개, clear, 실행 없는 cancel, 기본 hook disable을 검사했다. 실제 리뷰 상태 화면·최종 VSIX 검증의 완료 증거로 확대하지 않는다.

상세 기록과 host JSON은 CD의 `.documents/execution/preventive-review/P01.md` 및 `evidence/P01-C02-extension-host-installed.json`에 있다. Source·snapshot test는 합성 provider를 사용한다. API 삭제 fixture의 Python caller는 base에서 성공하고 삭제 후 실패했지만 이 결과를 실제 모델 탐지율로 계산하지 않는다.

## 유지한 경계

원래 `/Users/a09255/git/commit-defender`의 변경은 그대로다. `AGENTS.md` SHA-256은 `90609bad8e6cecaf1dd5588888c749a3c8aed62d76641da2de1c5fc2c0bc5d1e`, phoenix submodule의 binary diff SHA-256은 `1e363b2294e62a361aa175f196cab9bc325dc62ac53cbda5b8ea97f4901e1c1a`로 P00과 같다.

Snapshot 테스트는 일반·v4·split·sparse·alternate·linked-worktree index의 원본 byte·mtime 보존을 확인한다. 임시 index에서 생성한 tree object는 일반 Git GC 대상이며 영구 이력 저장소가 아니다. Prompt 수집과 marker 판정은 고정됐지만 legacy CLI의 별도 도구 호출은 아직 원래 cwd를 사용한다. Source 읽기 전체 격리는 P02-C03/C05에서 구현한다.

이번 변경은 CD extension/hook과 실행 문서에 한정된다. GCR 서버 runtime/배포 설정이 바뀌지 않아 PRISM-DEV에 새 image를 배포하지 않았다. CD Marketplace 게시는 P09에서 고정 VSIX의 Development Host·설치·화면·publisher 인증 검증 뒤 수행한다. 기존 hook·provider 설정을 초기화하거나 신규 자동 trigger를 켜지 않았다.

## 다음 commit과 완료 조건

P01의 코드 commit 다섯 개는 완료됐다. 실제 CD 리뷰 화면에서 상태·finding 이동·clear·cancel을 확인해야 P01 전체를 완료할 수 있다. 이와 독립적으로 P02-C01의 공통 계약과 이어지는 local store·source/executor를 구현한다. P02 이후 공통 core·인증·동기화·자동 리뷰·배포와 후속 phase의 원래 범위는 유지한다.

## C03 검증과 GUI 대기 조건

기존 31개 검사와 신규 outcome 23개 검사가 통과했다. C03의 최종 `npm run typecheck:test`, `npm run build`, `git diff --check`도 통과했다. 신규 검사는 전부 provider 실패, 일부 실패 후 취소/timeout, 보존된 finding·미수행 파일, 입력/JSON 잘림, API 완료 metadata 누락, HTTP response body 읽기 중 취소, legacy/advisory 판정과 실제 hook bundle 출력을 확인한다. 합성 provider이며 실제 모델 품질 검증은 아니다.

`npm run test:outcome-ui`로 실제 VS Code에서 네 상태의 화면과 Raw JSON을 확인하는 runner를 추가했다. Mac이 잠겨 컴퓨터 제어 도구가 접근하지 못했고 첫 Raw JSON 조작 대기가 240초 후 timeout되어 command가 exit 1로 종료됐다. 실제 화면을 확인하거나 성공 evidence를 생성하지 않았다. 사용자에게 잠금 해제를 요청한 상태다. 잠금 해제 후 최종 P01 source로 재실행해야 하며 단순한 제어 파일 ack 생성으로 화면 검증을 대신하지 않는다. 절차는 CD `vscode-extension/test/UI_VERIFICATION.md`에 있다.

GUI 대기는 C04/C05의 코드 작업을 막지 않는다. 이 상태를 전체 goal의 blocked나 P01 완료로 기록하지 않는다. C03에서도 GCR runtime/배포 설정과 Marketplace 배포본은 바뀌지 않았다.

## C04 검증과 전달

CD `2cc3dde`를 push했다. 기존 54개와 보안 9개가 통과했고 Markdown 의존성을 반영한 build·typecheck 및 Node 18.20.8의 security 9개를 확인했다. 실제 GUI는 Mac 잠금 상태가 다시 확인되어 미검증으로 남겼다. 기존 CD checkout의 두 보존 hash는 위 기록과 같다. C04는 CD extension/hook 변경이며 GCR runtime·배포 설정과 Marketplace 공개본은 바뀌지 않았다. 실제 개발·배포 재개 요청에 따라 현재 세션에서 개발·배포를 계속 진행한다.

## C05 검증과 전달

CD `21f6aa2`를 push했다. 전체 `npm test` 73개, 최종 typecheck/build, Node 18.20.8의 backend/ownership 10개가 통과했다. Clear/link 무효화 보완 후 security 9개도 통과했다. 리뷰와 커밋 메시지를 단일 backend 선택점으로 모으고 source·설정·Skill을 준비 시점에 고정했다. 동일 요청의 합류와 오래된 실행의 progress/result/error/finally 차단을 합성 CLI와 비협조적인 지연 응답 fixture로 검사했다. Extension process 간 영속 scheduler와 독립 CLI source 읽기 격리는 각각 P07과 P02의 후속 범위다.

Mac 잠금으로 실제 GUI 검증은 미완료다. Source commit·push와 서버 rollout·Marketplace 게시를 구분하며 이번 checkpoint에서 배포 완료를 선언하지 않는다. 현재 변경은 CD extension/hook이며 GCR 서버 runtime·배포 설정은 그대로다. 원래 P00–P13 개발·배포 범위와 P09의 최종 artifact·실제 설치·게시 gate는 유지한다.
