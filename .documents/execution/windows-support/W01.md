# W01 — Windows 네이티브 수동 리뷰

상태: 진행 중. 실제 Windows Codex 격리와 Extension Host 리뷰를 통과하기
전에는 완료로 표시하지 않는다. W02–W04는 시작하지 않는다.

## 시작 상태와 보존

2026-09-16 원격 fetch 후 GCR `f5d0f41`, CD `35b4874`가 지정 branch의
최신 commit임을 확인했다. 두 checkout은 clean이었다. 기존 Windows
branch가 없어 각각 `codex/windows-native-support`를 생성했다.

- GCR: `C:\Users\pydemia\git\git-code-reviewer`
- CD: `C:\Users\pydemia\git\commit-defender`
- Windows 11 Pro build 26200 ARM64, Snapdragon X1E80100.
- Node 24.16.0 ARM64, VS Code 1.137.0 ARM64, Codex CLI 0.153.4.
- GCR은 고정 pnpm 10.17.1의 frozen lockfile 설치, CD는 `npm ci`를 사용한다.
  전역 pnpm/Node/Codex를 교체하지 않는다.

[시작 근거](evidence/W01-start.json)에 전체 source SHA와 설정 hash를 남긴다.
G03/G04의 최종 실제 모델 성공, 이전 timeout/partial, 고정 artifact
alpha.42/service alpha.37/VSIX 2.11.3의 구분을 확인했다. 해당 macOS
결과를 Windows 성공 근거로 재사용하지 않는다.

## OS primitive 결정

Windows Credential Manager의 사용자 credential set을 사용한다.
master key와 reader key는 기존 service namespace를 구분하며 모델 API
credential은 기존 destination-bound 암호화 record와 별도 master key를
유지한다. read/write/remove는 고정 helper의 bounded stdin/stdout으로
전달한다. 평문 파일, argv, 환경변수로 secret을 전달하지 않는다.
missing과 접근 불가·손상·잠금을 구분하고 plaintext fallback은 없다.

helper는 저장소에 소스를 둔 .NET Framework 프로그램으로 빌드한다.
Windows 기본 .NET Framework 4.x와 Win32 API를 사용하며 별도 runtime
설치·관리자 실행·PowerShell 실행 정책 완화는 요구하지 않는다.
배포 artifact에 소스·Apache-2.0 license·version·hash를 포함한다.
이 machine의 ARM64 실행을 먼저 검증하고 x64는 별도 검증으로 남긴다.

데이터 기본 경로는 OS가 반환하는 사용자 LocalApplicationData 아래
CommitDefender다. 로컬 고정 NTFS volume만 허용한다. 신규 디렉터리는
생성 시점에 사용자/SYSTEM/Administrators만 허용하는 보호 DACL을
설정한다. 기존 경로의 권한을 자동 수정하지 않는다. reparse point와
비허용 SID 권한을 거절한다. 파일은 handle 검증·공유 제한을 적용하고
동일 volume의 배타적 게시를 사용한다. 파일 flush와 directory fsync의
차이는 검증 결과에 명시한다.

프로세스 수명은 kill-on-close Job Object에 실행 전에 가입시켜 관리한다.
전역 프로세스 이름별 종료는 하지 않는다. Codex 전역 지침 차단 방식은
설치된 CLI의 합성 loopback marker 검증을 통과하기 전 활성화하지 않는다.

다른 일반 사용자 접근 시험은 임시 계정 생성 허용 여부를 확인 중이다.
기존 CodexSandbox 계정을 변경하거나 비밀번호를 추출하지 않는다.

## 검증 대상과 실제 모델 예산

저장: 사용자 credential 재조회·삭제·오류, NTFS 권한/reparse 거절,
동시 생성/CAS, 손상·중단 복구, 한국어/공백 경로, snapshot 고정 및
민감 파일 제외를 확인한다. 실행: timeout/취소/부모 종료/자식·손자 정리와
독립 프로세스 보존, 실제 도구 catalog와 전역 지침 marker 차단을 확인한다.

실제 모델 초기 한도는 최소 결함 fixture **1회, 최대 240초**다.
기존 기본 Codex 계정의 `gpt-5.6-luna / high`를 사용하며 임의 대체하지
않는다. 중앙 연결 없는 별도 Windows Extension Host에서 고정한
source/base/caller/경계값 테스트를 제공한다. 구체적인 fixture와 기대
finding은 호출 전에 기록한다. 성공은 `completed` 결과, 결함 지적,
동일 저장 결과 재조회가 모두 성립한 경우다. timeout/partial/실패는
성공에 포함하지 않는다. 합성 provider 응답과 실제 계정 호출을 분리한다.

추가 실제 호출 전에는 원인·수정·재검증 대상·추가 횟수와 timeout을
기록한다. 설치 VSIX, 검증 Host, 이미 열린 사용자 Host의 상태를 구분한다.
전역 계정·모델·자동 실행 설정을 보존하고 사용자 창을 재로드하지 않는다.

## 참조

- [Windows Credential Manager](https://learn.microsoft.com/en-us/windows/win32/api/wincred/nf-wincred-credwritew)
- [Job Object 수명과 후손](https://learn.microsoft.com/en-us/windows/win32/procthread/job-objects)
- [CreateFile 공유·생성 규칙](https://learn.microsoft.com/en-us/windows/win32/api/fileapi/nf-fileapi-createfilea)

확인된 완료/실패 근거와 artifact 연결은 후속 검증 후 추가한다.
