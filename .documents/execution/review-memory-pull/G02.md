# G02 — 과거 리뷰 이력 저장·중앙 조회·읽기 API

상태: 진행 중. 기준 source `ec07591`, branch `codex/review-memory-pull-g01`, 작업 트리 `/Users/a09255/git/git-code-reviewer-g01`.

## 시작 상태와 구현 범위

G01 실행 기록을 확인했다. PRISM-DEV는 runtime `0.8.0-alpha.61`, chart `0.10.57`, Helm revision 71이며 server·worker Ready다. 원래 checkout과 별도 runner WIP는 변경하지 않는다.

2026-09-15 17:01 KST 조회에서 활성 저장소 2곳의 PR 1,189건과 대화 244건을 확인했다. 원문·본문 버전·REST/스레드 관측 이력은 기존 `github_pr_messages`, `github_pr_message_versions`, `github_pr_message_observations`에 저장돼 있다. 기존 화면은 분석 결과에 종속되고 reader credential에 이력 읽기 경로가 열려 있지 않다. 기본 polling은 열린 PR과 최근 닫힌 PR만 따라가므로 지정한 과거 PR의 제한된 재수집 경로가 필요하다.

- C01: 기존 job 실행권·재시도와 메시지 저장을 재사용한다. 수집 요청은 이미 등록된 저장소의 명시한 PR 번호 최대 20개로 제한한다. PR 단위 완료·재시도 위치를 저장하며 REST의 기존 페이지 상한을 유지한다. 전체 조회가 성공한 경우에만 수집 범위와 최근 조회에서 반환되지 않은 코멘트를 기록한다. 반환되지 않음은 삭제의 확정 증거로 표현하지 않는다. 원문·과거 관측은 보존한다.
- C02: 저장소별 이력 목록·PR 원문·답글·관측 이력·수집 범위를 페이지 조회로 제공한다. 기존 `knowledge:read` credential와 저장소 reader 인가를 재사용한다. 읽기 요청에는 서버 소유 ID·cursor·revision만 허용하며 원문을 검색 요청으로 업로드하지 않는다. 분석이 없어도 중앙 화면에서 읽을 수 있게 한다.
- C03: 공용 지침을 개인 후보 승격 없이 원문에서 직접 작성한다. 기존 collective memory와 publication projection을 사용하고 저장소 관리자의 한 번의 명시적 활성화로 발행을 요청한다. source hash/관측이 바뀌거나 원문이 최근 조회에 없으면 재검토가 필요하다. 기존 개인 자료는 공용으로 바꾸지 않는다. Skill bundle 계약을 유지한다.
- D01: 실제 대상과 GitHub를 대조하고 출처 지침 1건을 활성화·발행해 reader 조회를 확인한다. 필요한 runtime/web/migration만 Helm 배포한다. 문서·코드·배포 pin·실행 결과를 구분해 commit·push한다.

사용자가 추가 수집·실제 대조 대상으로 `skccmygit/skax-successionX-backend` PR #917·#915를 선택했다. 실제 수집 범위는 이 두 PR로 고정한다. 기존 API의 endpoint별 최대 20페이지·페이지당 100건 상한을 유지한다. 신규 PR은 생성하지 않으며 시험용 댓글이나 Check도 게시하지 않는다.

## 제외 범위

CD 구현·설치, runner, 중앙 모델 대행, local source/result/feedback/chat 업로드, G03·G04는 진행하지 않는다. 원문 저장·조회에 승인 단계를 추가하지 않는다. resolved·merged·승인을 결함 수정 증거로 간주하지 않는다. 합성 검증과 실제 원천 대조 범위를 구분하고 계정 비밀이나 불필요한 원문을 실행 기록에 복제하지 않는다.

## 구현·회귀 검증

- `057f52d`: C01 제한 수집·job 복구·수집 완료 범위·최근 조회 부재 관측.
- `89b6db9`: C02 중앙 `/review-history` 화면과 기존 reader credential의 GET API.
- `36a17bc`: C03 공용 원문에서 직접 작성한 collective 지침, 한 번의 활성화·발행, 출처 변경 시 재검토·제외.
- `7766425`: 상세 관측 기능 이전에 저장한 본문 버전의 조회 누락 보완. 기존 `github_pr_message_versions`를 그대로 읽는 API와 화면을 추가했다. 새 migration은 없다.

원문·답글·본문 버전·관측은 승인이 없어도 읽는다. 새 공용 지침은 개인 후보나 집단 정족수 승격을 거치지 않는다. 기존 `review_memories`, memory event, publication projection/outbox와 manifest·bundle을 재사용했다. reader GET 이외의 수집·작성·활성화는 중앙 브라우저의 저장소 관리 권한으로 제한했다. [읽기 계약과 사용 방법](../../review-history-reader-api.md)을 따른다.

전체 package/runtime/web 빌드와 변경 TypeScript ESLint·diff 검사가 통과했다. 회귀 검사 10개 파일 145건, 추가 수집·client 호환 검사 5개 파일 65건이 통과했다. 본문 버전 보완 후 이력 검사 10건을 다시 실행했다. 마지막 10건 중 9건은 이전 이력 검사의 반복이며 1건은 상세 관측 없이 저장된 옛 본문·페이지 조회 검사다. 테스트 횟수를 기능 진행률로 계산하지 않는다.

검사에는 만료된 수집 실행권 회수, 늦은 응답의 덮어쓰기 방지, pagination 상한 실패 시 이전 원문 보존, 실제 reader credential 인증·쓰기 차단·권한 철회, 다른 PR·개인 자료 차단, 출처 변경 시 발행과 리뷰 보강 제외가 포함된다. 1360px·420px 브라우저에서 원문을 열고 지침 초안 작성·활성화, reader의 관리 버튼 부재를 확인했다. 로컬 PostgreSQL과 테스트 환경은 정리했다. 합성 데이터 검증이며 실제 계정의 운영 API 검증과 구분한다. [검증 기록](evidence/G02-verification.json)을 따른다.

## 배포와 데이터 대조

runtime `0.8.0-alpha.62` / chart `0.10.58`를 Helm revision 72로 배포했다. 배포 후 마지막 점검에서 과거 본문 버전의 화면 조회 누락을 발견했고 `7766425`로 보완했다. 최종 runtime은 `0.8.0-alpha.63`, chart는 `0.10.59`, Helm revision은 `73`이다. 배포 pin commit은 각각 `d274a0d`, `c624429`다.

최종 image는 `sha256:b9d176b5eebaa26cadf19325a18a851405f50dd8fa53637c6781282f3f19993f`, source는 `776642502607bf4a9d099f1143a2faa243cb0392`다. OCI chart digest는 `sha256:80f39093e8ef029e66fba8225703d82e34dcbd58b0d1eb06eda4f1ceeedc6499`다. 소스 commit의 `git archive`로 rootless BuildKit에서 빌드했고 registry digest·label·실제 이미지 파일을 대조했다.

DB schema는 52에서 53으로 변경됐다. `0053_review_history_collection.sql`만 추가했으며 이전 migration checksum은 유지했다. 두 번의 Helm dry-run과 배포 전후 대조에서 비이미지 설정, Secret·HTTPRoute·PVC, 사용자·권한·계정·기존 분석·보고서·메모리 행을 보존했다. 인증 `local`, DB TLS `verify-full`, 기존 provider 설정을 유지했다. 최종 server·worker Ready, Keycloak 2개 Ready, HTTPS 상태·웹 asset·실행 모듈 hash와 Helm 연결 검사가 통과했다. [최종 배포 대조](evidence/G02-deployment.json)를 따른다.

17:53 KST 독립적인 읽기 전용 원천 대조에서 다음을 확인했다. 배포 후 기존 대화 polling이 선택한 두 PR을 성공적으로 갱신했다. 신규 PR·시험 댓글·Check를 생성하지 않았다.

| PR | GitHub 현재 대화 | 저장 대화 | 현재 답글 | 저장 본문 버전 | 상세 관측 |
| --- | --- | --- | --- | --- | --- |
| #917 | 45 | 46 | 10 | 58 | 46 |
| #915 | 16 | 16 | 3 | 17 | 16 |

현재 GitHub의 61건 모두 저장 본문·본문 hash·답글 관계·원문 URL과 일치했다. #917의 inline 코멘트 `3964581198`은 현재 응답에 없지만 저장 원문·버전은 보존하고 `not-returned`로 기록했다. 삭제 원인을 확정하지 않는다. 수집 전 수정·삭제 내용은 복원했다고 표시하지 않는다. [원천 대조](evidence/G02-source-comparison.json)를 따른다.

원래 checkout의 runner WIP 11개 파일은 보관 hash와 일치한다. CD checkout은 `8bb3916`, clean이며 client package·CLI·CD 소스나 설치를 변경하지 않았다. [작업 보존](evidence/G02-workspace-preservation.json)을 확인했다. 이번 빌드·이미지 검사에 만든 Kubernetes 자원은 제거했다. [자원 정리](evidence/G02-resource-cleanup.json)를 따른다.

## 남은 실제 인증·활성화 검증

상태: G02 완료 전 검증 대기. 유효한 GCR 로그인 정보가 필요하다. 배포 설정의 기존 bootstrap 관리자 비밀번호가 현재 저장된 계정 hash와 일치하지 않아 로그인 HTTP 요청을 수행하지 않았다. 비밀번호·권한·기존 사용자 세션을 변경하거나 인증을 우회하지 않았다. 사용자에게 로그인 정보가 담긴 로컬 파일 경로를 요청했다.

발행할 지침은 PR #917 코멘트 `3967869279`에 연결했다. 내용은 BlockUpdateRequest의 요청 값 검증을 Pydantic validator에서 수행하되 DB·권한·저장 상태가 필요한 검증은 제외하는 것이다. [출처와 준비한 초안](evidence/G02-guidance-draft.json)을 남겼으며 아직 운영 DB에서 활성화하거나 발행하지 않았다.

유효한 로그인으로 기존 reader API key를 발급해 운영 GET·쓰기 차단·key 회수, 실제 원문·본문 버전 페이지, 명시한 두 PR의 수집 작업을 확인한다. 이어서 지침 1건을 활성화하고 서명된 manifest·collective bundle의 해당 지침 및 기존 policy bundle을 대조한다. 검증용 key와 새 로그인 세션만 정리한다. 준비한 실행 스크립트는 무시된 `artifacts/operations/G02-history-final/live-proof.mjs`다.

G02를 완료 처리하지 않았으며 G03·G04, runner, CD 변경은 시작하지 않았다.
