# G01 — WIP 정리와 기존 GCR 동작 복구·보존

상태: 실행 중. G02와 runner 개발은 포함하지 않는다.

## G01-W01 — 보관·분리 완료

기준 source는 `f8d2048`이다. 미완성 runner의 수정 파일 4개와 신규 파일 7개를 `codex/wip-local-check-runner`의 `82c2436a4e4d71620c72d20c48bede220fac9d6b`에 보관했다. 원래 checkout의 변경은 유지했다.

11개 파일 각각의 SHA-256을 Git blob과 `git archive` 복원본에 대조했다. 실행 worktree `/Users/a09255/git/git-code-reviewer-g01`, branch `codex/review-memory-pull-g01`은 기준 source에서 만들었다. 수정됐던 CLI와 package index 4개가 기준 commit과 일치하고 신규 runner 파일 7개는 존재하지 않는다. 이전 node_modules·dist·tarball은 복사하지 않았다.

보관은 runner 기능 완료가 아니다. 과거 합성 테스트 27건 통과 기록은 남기되 실제 Docker 실행 검증·패키징·설치·배포는 미완료로 유지한다. [파일별 복원 검증](evidence/G01-W01.json)을 따른다.

## G01-C00 — 문서 전달

원래 요구 범위 점검·조회 증거, 새 구현 계획과 이전 문서의 대체 안내를 실행 branch에 문서만으로 전달한다. WIP source는 이 branch에 merge하지 않는다. 실제 commit·push와 원격 일치는 후속 검증 기록에 남긴다.

## 다음 작업

worker 장애 원인을 확인하고 만료된 작업의 복구·종결을 검증한다. 기본 PR 분석과 선택적인 중앙 메모리 보강을 분리하고 필요한 runtime만 배포한다. 기존 데이터·계정·provider·TLS·CD 설치를 보존한다.
