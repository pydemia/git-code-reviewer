# GCR·Commit Defender 재배포 — 2026-09-16

사용자의 설치·배포 요청에 따라 GCR을 PRISM-DEV에 Helm upgrade하고 이 Mac에 Commit Defender 2.12.6 darwin-arm64를 설치했다. 기존 Windows 작업의 완료·게시 기록을 확인한 뒤 `codex/windows-native-support`의 원격 변경을 fast-forward했다. GCR 배포 전 HEAD는 `ad5bcea`, CD는 `4d1b237`이었다.

## GCR

| 항목 | 배포 결과 |
| --- | --- |
| 접속 주소 | https://pr-review.prism.ai |
| context / namespace | PRISM-DEV / git-code-reviewer |
| app / chart | 0.8.0-alpha.64 / 0.10.60 |
| Helm revision / 상태 | 74 / deployed |
| image source | `67ec73c0e64b7846976efb7342b5676338773070` |
| 배포 pin source | `73014b7cb819fa4b156dc1712c6419f4c5cb2a56` |
| image digest | `sha256:1cbe141934bdc2bc0ac6e162419a505004f03e8c751e60a58f30c3d1e0f767d5` |
| OCI chart digest | `sha256:3607fb6417957db4a5270a2b150d4f85192e4326ea6b5798960a8ac8736d3bd9` |

PRISM-DEV 노드에 맞춰 Linux amd64 image를 게시했다. 일반 다중 아키텍처 release 검증을 새로 수행한 것으로 해석하지 않는다. 기존 alpha.63이나 chart 0.10.59를 덮어쓰지 않았으며, 새 chart를 OCI에서 다시 받아 원본 tgz와 byte 단위로 대조했다.

최신 client-core build가 참조하는 `scripts/build-windows-native.mjs`를 Docker build context에 COPY하도록 수정했다. Linux에서는 이 script가 native Windows build를 건너뛴다. 작업용 artifact가 image build context에 포함되지 않도록 `.dockerignore`에도 제외했다. 서버 runtime 코드와 DB migration은 기존 배포 source `7766425`에서 변경되지 않았다. 최신 UI·문서와 공통 package source를 포함해 전체 Docker build를 수행했다.

Docker Desktop 내부 containerd I/O 오류는 일반 재시작 후 해소됐다. factory reset, image/volume prune, 데이터 삭제는 수행하지 않았다. 응답하지 않던 이 작업의 registry probe를 종료하고 작업 전용 Docker config에서 기존 macOS Keychain credential helper를 사용했다. 전역 Docker 설정은 바꾸지 않았다. Alpine TLS 검증에는 기존 신뢰 CA와 system root 묶음을 BuildKit secret으로 전달했으며 TLS 검증을 끄지 않았다.

Helm 기존 values를 재사용했다. 렌더링 대조에서 버전 label·image·관련 config checksum 외의 변경이 없었다. 배포 후에도 image 항목을 제외한 Helm values는 이전과 같다. PostgreSQL과 별도 identity Helm release를 재설치하거나 변경하지 않았다.

## 검증과 데이터 보존

- Server 1/1, Worker 1/1 deployment가 새 generation과 image로 Ready. Worker Pod의 worker·source-sandbox 두 container도 Ready.
- Helm connection test Succeeded. HTTPS 인증서 검증을 유지한 startup/live/ready·system·웹 HTML 응답은 200이며 system version은 alpha.64다. HTML의 JS/CSS asset 다운로드도 성공했다.
- 익명 `/api/v1/repositories` 접근은 401로 거절됐다. 처음 시도한 목록 URL `/api/v1/analyses`는 존재하지 않아 404였으며 실제 등록된 route로 검사 대상을 바로잡았다.
- 사용자 7명, chat account 7건, analysis 192건, report 184건, reader grant 2건, tenant membership 7건, review memory 5건, local credential 6건, knowledge release 10건, history collection 1건의 기존 행 해시가 모두 보존됐다. rule/revision은 이전과 같이 0건이다.
- migration 53개와 checksum이 같다. DB 연결은 `verify-full`, 실제 세션은 TLSv1.3이다.
- Secret 12개의 UID·data, ConfigMap 7개의 UID·data, PVC 2개의 UID·volume, HTTPRoute 4개의 UID·spec이 유지됐다. identity release revision 2도 그대로다.

이전 worker Pod는 기존 sandbox broker의 SIGTERM 후 650초 유예로 검사 당시 Terminating 상태였다. 해당 worker의 leased job은 0건이며 강제 삭제하지 않았다. 새 worker의 health/live·ready는 모두 200이다. 종료 유예는 기존 동작이며 이번 배포에서 변경하지 않았다.

검증용 실제 모델 호출은 0회다. 기존 G01~G04·Windows/macOS 회귀 증거를 재사용했으며 추가 리뷰·PR 댓글을 만들지 않았다. 새 이미지 빌드와 이번 운영 검사는 전체 기능 회귀를 다시 실행했다는 의미가 아니다.

## Commit Defender 설치

Mac의 VS Code 1.137.0에 Marketplace의 기존 게시본 2.12.6 darwin-arm64를 설치했다. 설치 전은 2.11.3이었다. 새 Marketplace 게시와 VSIX 재빌드는 하지 않았다. 설치된 runtime 11개 파일의 SHA-256이 기존 게시 artifact와 일치하며 별도의 실제 Extension Host에서 활성화와 명령 26개 등록을 확인했다.

- CD 제품 source: `3be0dc0ff3e243bb8e8fa7aa4fcac254d9ad7977`; packaging source: `3a39933b64750e89b8e5dc793bf4d0ab24cff124`.
- 공통 artifact source: `2958d916abbca7f598818e6821db64349c20d3d1`; client-contract alpha.48 / client-core alpha.49 / executors alpha.50 / private CLI·service alpha.39.
- CD 설치 기록 commit: `0c783f9`, `.documents/execution/windows-support/macos-install-2026-09-16.md`.

사용자의 VS Code settings.json 및 Codex config.toml·auth.json은 검사 전후 해시가 같다. provider·모델·자동 실행 설정과 전역 CLI를 변경하지 않았다. 시험용 Host profile·workspace·Keychain 참조를 정리했다. 이미 열린 사용자 Host는 강제 reload하지 않았고 그 Host가 실행 중인 버전은 확인하지 않았다. 사용자가 작업을 저장한 뒤 `Developer: Reload Window` 또는 정상 재시작하면 새 설치본이 적용된다.

[기계 판독용 검증 근거](evidence/redeploy-2026-09-16.json)에 source·package hash·배포 상태와 보존 결과를 함께 기록했다. 계정 원문과 DB 행 원문은 포함하지 않는다.
