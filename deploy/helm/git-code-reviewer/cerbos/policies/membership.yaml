apiVersion: api.cerbos.dev/v1
resourcePolicy:
  resource: membership
  version: default
  rules:
    - actions: ['*']
      effect: EFFECT_ALLOW
      roles: ['administrator']
      condition:
        match:
          expr: request.principal.attr.enabled == true
