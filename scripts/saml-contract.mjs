// The adoption fixtures and application use one protocol implementation.
// Run through the package scripts (tsx) on every supported Node 22 release.
export * from '../apps/runtime/src/auth/saml-protocol.ts';
